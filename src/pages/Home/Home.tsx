import { Button } from "@ahaui/react";

const Home = () => {
  return (
    <>
      <div>Home</div>
      <Button onClick={() => console.log("hello")}>Example button</Button>
    </>
  );
};

export default Home;
